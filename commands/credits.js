const embedHelper = require("../modules/embedHelper.js");

exports.run = (client, message, args) => {
  //let embed = embedHelper.createEmbed("credits", client);
  //return message.channel.send(embed);
}