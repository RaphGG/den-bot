module.exports = {
  name: "Pokèmon Type Match-Up Command",
  cmdName: "matchup",
  aliases: ["type", "types", "matchups"],
  description: "Shows a detailed summary of type matchups for the given type.",
  args: 1,
  usage: "{{prefix}}mathcup [Pokémon Type 1] (Pokémon Type 2)",
  example: "{{prefix}}matchup fire flying",
  guildOnly: false,
  adminOnly: false,
  run(client, message, args, settings) {
    run(client, message, args, settings);
  }
};

const run = (client, message, args, settings) => {
  
};